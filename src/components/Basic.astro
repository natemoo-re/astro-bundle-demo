---
import { Store } from "astro:store";

interface Props {
  data: any;
}

const { data } = Astro.props;

const store = new Store("data", { asset: true });
const asset = await store.set("[hash].json", JSON.stringify(data, null, 2));
---

<pre>{asset}</pre>
